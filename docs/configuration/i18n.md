# Languages

One config, N languages. No duplicate files.

## Declaring locales

```yaml
# site.yaml
locales: [fr, en]
```

The first entry is the default: it is the fallback for missing translations
and for visitors whose browser language is not served. Locale codes appear in
URLs (`/fr/`, `/en/`) and match browser languages by base tag (`fr-CA`
visitors get `fr`).

## How a visitor gets a language

1. Their saved choice (a cookie set when they use the header switcher — or
   visit any locale URL).
2. Otherwise the browser's `Accept-Language`.
3. Otherwise the default locale.

`/` redirects accordingly; every page also links its siblings in the other
locales, so search engines index each language separately.

## Translated fields

Every text field (`name`, `desc`, `details`, `tagline`, category names,
footer labels, UI strings) accepts either form:

```yaml
name: PDF toolbox                                # every locale
name: { fr: Boîte à outils PDF, en: PDF toolbox } # per locale
```

Lookup order: the visitor's locale → the plain-string value → the default
locale → any value rather than nothing.

## UI strings

The interface text ships with French and English built in. Other locales fall
back to English until you override them — `strings` in `site.yaml` accepts
any locale:

```yaml
# site.yaml — cairn in Dutch
locales: [nl]
strings:
  nav.skip: Naar de inhoud
  nav.languages: Taal
  search.label: Zoeken
  search.placeholder: Zoek een tool…
  search.empty: Geen resultaten. Probeer een ander woord.
  cat.other: Overig
  card.more: Meer info
  detail.open: Open de tool
  detail.back: Terug
```

| Key                  | English default             | Where                       |
| -------------------- | --------------------------- | --------------------------- |
| `nav.skip`           | Skip to content             | keyboard skip link          |
| `nav.languages`      | Language                    | switcher label (a11y)       |
| `search.label`       | Search                      | search input label (a11y)   |
| `search.placeholder` | Search for a tool…          | search input                |
| `search.empty`       | No results. Try another word. | empty search state        |
| `cat.other`          | Other                       | implicit category           |
| `card.more`          | Learn more                  | card link to detail page    |
| `detail.open`        | Open the tool               | detail page button          |
| `detail.back`        | Back                        | detail page back link       |

A partial override only applies to the locales it names; the built-ins fill
the rest.

Next: [Docker Compose](../deployment/docker-compose.md)
